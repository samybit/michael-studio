---
trigger: always_on
---

Implement the mobile-first styling system defined in `DESIGN_SYSTEM.md`. Ensure that base classes handle the compressed, stacked single-screen layout for mobile phones, and use the `md:` modifier exclusively to deploy the horizontal split-pane desktop configuration.